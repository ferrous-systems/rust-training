[package]
name = "mdbook-striplineno"
description = "Strips linenumbers from code blocks in markdown"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
mdbook-preprocessor = "0.5.2"
serde_json = "1.0.149"
